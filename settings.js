'use strict';
const username = 'admin';
const password = 'admin';
const host = 'localhost';
const port = 8000;

const connection = {
  host: host,
  port: port,
  user: username,
  password: password
};

module.exports = {
  connection: connection
};
